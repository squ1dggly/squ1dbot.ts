import config_client from "./config_client.json";
import config_logger from "./config_logger.json";
import config_commands from "./config_commands.json";

export default { client: config_client, logger: config_logger, commands: config_commands };
